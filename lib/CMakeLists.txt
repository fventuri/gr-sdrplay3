# Copyright 2011,2012,2016,2018,2019 Free Software Foundation, Inc.
#
# This file was generated by gr_modtool, a tool from the GNU Radio framework
# This file is a part of gr-sdrplay3
#
# SPDX-License-Identifier: GPL-3.0-or-later
#

########################################################################
# Setup library
########################################################################
include(GrPlatform) #define LIB_SUFFIX

list(APPEND sdrplay3_sources
    rsp_impl.cc
    rsp1_impl.cc
    rsp1a_impl.cc
    rsp1b_impl.cc
    rsp2_impl.cc
    rspduo_impl.cc
    rspdx_impl.cc
    rspdxr2_impl.cc
    sdrplay_api.cc
)

########################################################################
# Find gnuradio build dependencies
########################################################################
find_package(LibSDRplay)
if(NOT LIBSDRPLAY_FOUND)
    MESSAGE(FATAL_ERROR "SDRplay API driver not found... Please visit sdrplay, download and install API driver version 3.x")
endif (NOT LIBSDRPLAY_FOUND)

set(sdrplay3_sources "${sdrplay3_sources}" PARENT_SCOPE)
if(NOT sdrplay3_sources)
    MESSAGE(STATUS "No C++ sources... skipping lib/")
    return()
endif(NOT sdrplay3_sources)

add_library(gnuradio-sdrplay3 SHARED ${sdrplay3_sources})
target_link_libraries(gnuradio-sdrplay3
    gnuradio::gnuradio-runtime
    ${LIBSDRPLAY_LIBRARIES}
    ${Boost_LIBRARIES}
  )
target_include_directories(gnuradio-sdrplay3
    PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../include>
    PUBLIC $<INSTALL_INTERFACE:include>
    PRIVATE ${LIBSDRPLAY_INCLUDE_DIRS}
    PRIVATE ${Boost_INCLUDE_DIRS}
  )
set_target_properties(gnuradio-sdrplay3 PROPERTIES DEFINE_SYMBOL "gnuradio_sdrplay3_EXPORTS")

if(APPLE)
    set_target_properties(gnuradio-sdrplay3 PROPERTIES
        INSTALL_NAME_DIR "${CMAKE_INSTALL_PREFIX}/lib"
    )
    add_custom_command(
        TARGET gnuradio-sdrplay3 POST_BUILD
        COMMAND ${CMAKE_INSTALL_NAME_TOOL} -change libsdrplay_api.so.3 @rpath/libsdrplay_api.so.3 "$<TARGET_FILE:gnuradio-sdrplay3>"
    )
endif(APPLE)

########################################################################
# Install built library files
########################################################################
include(GrMiscUtils)
GR_LIBRARY_FOO(gnuradio-sdrplay3)

########################################################################
# Print summary
########################################################################
message(STATUS "Using install prefix: ${CMAKE_INSTALL_PREFIX}")
message(STATUS "Building for version: ${VERSION} / ${LIBVER}")

########################################################################
# Build and register unit test
########################################################################
include(GrTest)

# If your unit tests require special include paths, add them here
#include_directories()
# List all files that contain Boost.UTF unit tests here
list(APPEND test_sdrplay3_sources
)
# Anything we need to link to for the unit tests go here
list(APPEND GR_TEST_TARGET_DEPS gnuradio-sdrplay3)

if(NOT test_sdrplay3_sources)
    MESSAGE(STATUS "No C++ unit tests... skipping")
    return()
endif(NOT test_sdrplay3_sources)

foreach(qa_file ${test_sdrplay3_sources})
    GR_ADD_CPP_TEST("sdrplay3_${qa_file}"
        ${CMAKE_CURRENT_SOURCE_DIR}/${qa_file}
    )
endforeach(qa_file)
